- 所有涉及用户交互的页面（如新建、编辑）均在入口处通过 `getSession` 校验登录状态，未登录则重定向至 `/login`。
- 表单提交统一使用原生 `<form>` 标签配合 Server Action（如 `createPost`, `updatePost`）作为 `action` 属性值。
- 列表页与详情页的数据获取均包裹在 `try-catch` 或直接 `await` 调用中，并在数据缺失时调用 `notFound()` 或返回空状态。
- 教学类页面（`learn/*`）采用统一的布局结构：顶部标题与简介，中部通过卡片或代码块展示知识点，底部提供最佳实践总结。