- 采用 Next.js App Router 文件系统路由结构，`blog` 子目录实现 CRUD 业务逻辑，`learn` 子目录提供静态教学内容。
- 页面组件默认为服务端组件（Server Components），通过 `getSession` 和 `prisma` 直接在后端获取数据或执行操作。
- 依赖 `@/lib/actions` 处理表单提交与数据变更，依赖 `@/lib/session` 进行权限校验与重定向控制。
- 详情页使用 `react-markdown` 渲染内容，并通过 `incrementViews` 异步更新统计数据。